import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Actor2 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Actor2 extends Actor
{
    /**
     * Act - do whatever the Actor2 wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public Actor2()
    {
        setImage(new GreenfootImage("Cálculo", 24, Color.BLACK, null));
    }
}
